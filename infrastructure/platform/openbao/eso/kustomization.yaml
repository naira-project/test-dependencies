apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Synced by the `external-secrets` Argo CD Application (../application.yaml).
resources:
  - namespace.yaml
  - rbac.yaml
  - clustersecretstore.yaml
